@{
  Layout = "_Layout";
}

@model Library.Models.Book

<h2>Author Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Title): @Html.DisplayFor(model => model.Title)</h3>

@if(@Model.Books.Count == 0)
{
  <p>This author does not contain any books</p>
}
else
{
  <h4>Books the author contains:</h4>
  <ul>
  @foreach(var join in Model.Books)
  {
    <li>@join.Book.Title</li>
  }
  @if()
  </ul>
}

<p>@Html.ActionLink("Back to Authors List", "Index")</p>
<p>@Html.ActionLink("Edit Author", "Edit", new { id = Model.BookId })</p>
<p>@Html.ActionLink("Delete Author", "Delete", new { id = Model.BookId })</p>